# TODO_IdeaGraphRedesign

## 待办事项 / 后续优化建议

1. **高级搜索集成**:
   - 目前为了简化界面暂时移除了搜索框。后续可以重新集成搜索功能，通过 ECharts 的 `dispatchAction` 实现搜索结果的高亮显示。

2. **点击事件深层联动**:
   - 目前点击节点主要用于高亮连接关系。后续可以增加点击事件回调，点击节点后在侧边栏显示详细的笔记内容或跳转到编辑页面。

3. **更多布局算法**:
   - 引入更多布局算法（如层级布局、径向树图），适应更复杂的知识结构。

4. **性能优化**:
   - 如果节点数量超过 1000+，可能需要开启 ECharts 的 `large` 模式或使用 WebGL 版本 (`echarts-gl`) 进行渲染优化。
